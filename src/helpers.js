const {
    createPlayer,
    playerList
} = require("../index.js")

const fs = require("node:fs")
const {writeFileSync} = require("node:fs")

function writeJSON(path, fileName, data){
    return writeFilySyn
}